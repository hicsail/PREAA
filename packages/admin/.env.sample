LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=<LITELLM_KEY>

# Postgres (replaces the former MongoDB store). The app creates the `admin`
# database and its tables on boot if they don't exist.
DATABASE_URL=postgresql://psql:postgres@localhost:5432/admin

# Encryption key for secrets stored at rest (e.g. proxy API keys).
# 32 bytes as 64 hex chars: `openssl rand -hex 32`
ENCRYPTION_KEY=your_64_char_hex_key_here_generate_with_openssl_rand_hex_32

# CORS Configuration
# Comma-separated list of allowed origins for embedded chat client
# Example: ALLOWED_ORIGINS=http://localhost:3000,https://example.com
# If not set, defaults to '*' (allows all origins - use only in development)
ALLOWED_ORIGINS=*

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32

# Keycloak Configuration
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=myrealm
KEYCLOAK_CLIENT_ID=my-nextjs-client
KEYCLOAK_CLIENT_SECRET=your_keycloak_client_secret_here
